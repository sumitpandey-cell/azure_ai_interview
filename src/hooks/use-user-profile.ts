// Placeholder hook - backend removed, ready for fresh implementation
export function useUserProfile() {
  return {
    profile: null,
    loading: false,
    error: null,
    refetch: async () => { },
  };
}